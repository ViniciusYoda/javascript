var myText = 'I am a string';
var newString = myText.replace('string', 'sausage');
console.log(newString);
// the replace() string function takes a string, replaces one substring with another, and returns a new string witg the replacement made